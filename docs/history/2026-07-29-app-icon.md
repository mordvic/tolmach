> Historical record of the build. Where this and the code disagree, the code is right.
>
> Copied verbatim from the session ledger that was written while the plan was executed.
> It records what was found, what was ruled, and what was rejected — including defects in
> the plan itself. It is not maintained: it is the account of a build that has finished.

# SDD ledger — plan: docs/superpowers/plans/2026-07-29-app-icon.md

Branch: feat/app-icon (from feat/translation-engine)

Task 1: complete (commits 599984e..50ea5f6, review clean)
Task 1: minor (deferred): serifFont falls back to `?? probe` instead of throwing (Scripts/make-icon.swift:74-75) — plan-mandated, unreachable in practice
Task 1: minor (deferred): `CGColorSpace(name:)!` force-unwrap instead of the Failure pattern (Scripts/make-icon.swift:138) — plan-mandated, theoretical only

Task 2: complete (commits 50ea5f6..4c8c97f, review clean)
Task 2: minor (deferred): at 16 px the simplified chevron reads as a short stroke rather than a crisp angle — inherent to the spec's geometry at ~0.129 px/unit, verified separated from the letter by pixel dump; needs a human glance at real Finder/Dock rendering

Task 3: complete (commits 4c8c97f..9c08ba8, review clean)
Task 3: minor (deferred): CLAUDE.md:30 comment not column-aligned with shorter neighbours (matches the translate-cli line's pattern) — cosmetic
Controller: the plan omitted committing the spec and the plan themselves, leaving RUNBOOK.md pointing at an untracked file. Committed separately.

Final whole-branch review (opus, 599984e..9f90177): no Critical; 4 Important.
  I1 — spec §3.1 and the make-icon.swift comment state a 1.5-unit / 0.19 px gap
    that neither its own two anchors (36.5 - 35.5 = 1.0) nor the geometry
    reproduce; remeasured at 1.23 units = 0.159 px. Neighbouring figures also
    loose: final gap 6.90 (stated ~6.7), full-mark gap 2.79 (stated ~2.5).
    «Measured» is a contract here, so a number nothing reproduces hollows it out.
  I2 — serifFont's `?? probe` is the one path that draws a wrong-sized letter and
    exits 0, contradicting the fail-loudly comment eight lines below it.
  I3 — no docs/history/ ledger for this plan; the project keeps one per plan.
  I4 — the 16 px outcome (each guillemet renders as a 1x2 px cinnabar dash, letter
    peaks at mid-grey) was found, judged acceptable, and recorded nowhere.
Deferred-minor triage by the final reviewer:
  serifFont `?? probe` — promoted to Important (I2).
  CGColorSpace force-unwrap — accept; a trap is loud. Fix opportunistically.
  16 px chevron reads as a stroke — accept the geometry, fix the record (I4).
  CLAUDE.md comment alignment — accept, no change; the new line matches the
    existing convention for commands too long for the column.

Final-fix round: fixed all four Important findings from the whole-branch review above.
  I1 — corrected spec §3.1 and the make-icon.swift comment to the re-derived values:
    glyph-path left edge x ≈ 36.74, chevron's rightmost extent (the butt-capped end
    of the arm, not the miter — that join is at the apex, on the far left) x = 35.51,
    gap ≈1.2 units / 0.16 px at 16 px; reopened gap 6.90 units; full-mark gap 2.79
    units.
  I2 — serifFont now throws instead of `?? probe`-ing a 100 pt font at the target
    cap height. Also closed the same category one step further: after
    CTLineCreateWithAttributedString in drawIcon, the line is now checked for
    exactly one glyph run in exactly the font that was requested, and throws if
    CoreText silently substituted a different face — a substitution would
    invalidate the cap-height maths the whole sizing scheme rests on.
  I3 — this ledger.
  I4 — recorded the 16 px outcome (measured values above) in spec §7 as an
    accepted limitation.
  Also fixed opportunistically: the CGColorSpace force-unwrap now follows the
    Failure pattern; the redundant `setAllowsAntialiasing(true)` line was
    removed; the plan's Task 3 Step 1 expectation now names `_CodeSignature`;
    RUNBOOK.md's icon-regeneration description now covers the missing-file case,
    not only the newer-generator case; spec §4 now lists the script's real
    imports (AppKit, CoreGraphics, CoreText, Foundation, ImageIO); spec §7 now
    records that the icon is not byte-reproducible across machines, since the
    `.icns` is not committed and the mark depends on the system serif.
  Commit: fix(app): correct the icon's stated measurement and close its silent-failure paths

Scoped re-review of the fix wave: all 10 findings ADDRESSED, no new
  Critical/Important breakage.
Parked (no second fix wave) — the glyph-run check introduced two `as!` bridging
  casts (make-icon.swift:109,114) in the same commit that removed two force
  unwraps. Functionally inert: CFArray -> [CTRun], and a CFDictionary value the
  CoreText API contract guarantees is a CTFont. But it sits at odds with the
  file's now-explicit «throw Failure, never crash» discipline, and
  `guard let ... as? ... else { throw Failure(...) }` would have been free.
  Ruling: real, not load-bearing, deferred to whoever next touches the file.

Branch: 599984e..77e6198, 5 commits. `swift build --build-tests` clean, 0 warnings.
Never seen by anyone: how the icon renders in Finder, Spotlight, the Accessibility
  list, and against a dark desktop. Recorded in docs/OPEN-ITEMS.md §1.

Parked item revisited after merge review, and it was only half real.
  `CTLineGetGlyphRuns(line) as! [CTRun]` -> `as? [CTRun]` with a throwing guard:
    done, compiles, behaviour unchanged.
  `runAttributes[kCTFontAttributeName] as! CTFont?` -> `as? CTFont`: **not possible.**
    Swift rejects it outright — «conditional downcast to CoreFoundation type 'CTFont'
    will always succeed» is an *error*, not a warning, so the file does not compile.
    Forcing into an optional is the only form the language leaves, and it does not trap
    on an absent attribute: a nil subscript result casts to nil and the guard throws.
    The original code was right; the review finding was wrong on this half. A comment
    now says so at the call site so the next reader does not retry the change.
  Verified from scratch: build/ artifacts deleted, generator re-run — exit 0, zero
    warnings, ten rasters, iconutil round-trip clean.
